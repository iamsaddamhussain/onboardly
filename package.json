{
  "name": "onboardly",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"yarn dev:server\" \"yarn dev:client\"",
    "dev:server": "dotnet watch run --project server",
    "dev:client": "yarn --cwd client dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
