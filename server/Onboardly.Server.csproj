<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Onboardly.Server</RootNamespace>
    <!-- Email templates are embedded and rendered by RazorLight, not compiled as
         MVC views, so disable the default Razor view compilation. -->
    <EnableDefaultRazorGenerateItems>false</EnableDefaultRazorGenerateItems>
    <!-- Required by RazorLight: preserves the compile-time reference assemblies in
         the deps file so templates can be compiled at runtime. -->
    <PreserveCompilationContext>true</PreserveCompilationContext>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.0">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="10.0.0" />
    <PackageReference Include="RazorLight" Version="2.3.1" />
  </ItemGroup>

  <!-- Email Razor templates: embed them in the assembly so RazorLight can load
       them by name at runtime without shipping loose files. -->
  <ItemGroup>
    <Content Remove="Services\Email\Templates\**\*.cshtml" />
    <None Remove="Services\Email\Templates\**\*.cshtml" />
    <EmbeddedResource Include="Services\Email\Templates\**\*.cshtml" />
  </ItemGroup>

</Project>
